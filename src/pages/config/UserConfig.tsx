import { useEffect, useMemo, useState } from 'react'
import { App, Avatar, Button, Drawer, Form, Input, Popconfirm, Select, Space, Table, Typography, Upload } from 'antd'
import { DeleteOutlined, DownloadOutlined, UploadOutlined, UserOutlined } from '@ant-design/icons'
import { roleLabels } from '../../store'
import { PermissionGroup, User, UserRole } from '../../types'
import { api } from '../../api'
import { useAuth } from '../../auth'
import { compressImageFile, readImageFile } from '../../shared/image'

export default function UserConfig() {
  const { message } = App.useApp(); const { can, user: currentUser, refresh } = useAuth(); const [users, setUsers] = useState<User[]>([]); const [groups, setGroups] = useState<PermissionGroup[]>([]); const [loading, setLoading] = useState(true); const [selected, setSelected] = useState<User | null>(null); const [open, setOpen] = useState(false); const [form] = Form.useForm<User>(); const [keyword, setKeyword] = useState(''); const [checked, setChecked] = useState<number[]>([])
  const load = async () => { setLoading(true); try { const [userRows, groupRows] = await Promise.all([api.users(), api.permissionGroups()]); setUsers(userRows); setGroups(groupRows) } catch (error) { message.error((error as Error).message) } finally { setLoading(false) } }
  useEffect(() => { void load() }, [])
  const groupIdsFor = (user: User) => groups.filter((group) => user.groups?.some((item) => item.code === group.code)).map((group) => group.id)
  const edit = (user: User) => { setSelected(user); form.setFieldsValue({ ...user, avatarOriginal: user.avatarOriginal || user.avatar, groupIds: groupIdsFor(user), password: undefined }); setOpen(true) }
  const create = () => { setSelected(null); form.resetFields(); form.setFieldsValue({ code: undefined, name: undefined, password: undefined, role: 'user', groupIds: groups.filter((group) => group.code === 'platform_user').map((group) => group.id) }); setOpen(true) }
  const copyToClipboard = async (value: string | undefined, label: string) => {
    if (!value) return
    try {
      if (navigator.clipboard?.writeText) await navigator.clipboard.writeText(value)
      else {
        const textarea = document.createElement('textarea'); textarea.value = value; textarea.style.position = 'fixed'; textarea.style.opacity = '0'; document.body.appendChild(textarea); textarea.select(); document.execCommand('copy'); textarea.remove()
      }
      message.success(`${label}已复制`)
    } catch {
      message.error(`${label}复制失败`)
    }
  }
  const save = async (values: User) => { const { uuid: _uuid, ...editableValues } = values; try { if (selected) { await api.updateUser(selected.id, editableValues); if (selected.id === currentUser?.id) await refresh() } else await api.createUser({ ...editableValues, password: editableValues.password! }); setOpen(false); await load(); message.success('保存成功') } catch (error) { message.error((error as Error).message) } }
  const selectAvatar = async (file: File) => { if (!file.type.startsWith('image/')) { message.error('请选择图片文件'); return Upload.LIST_IGNORE } if (file.size > 10 * 1024 * 1024) { message.error('头像不能超过 10MB'); return Upload.LIST_IGNORE } try { const [thumbnail, original] = await Promise.all([compressImageFile(file, 100 * 1024, 256), readImageFile(file)]); form.setFieldsValue({ avatar: thumbnail, avatarOriginal: original }); message.success('头像已生成缩略图，保存后生效') } catch (error) { message.error((error as Error).message) } return false }
  const downloadAvatar = (url: string) => { const link = document.createElement('a'); link.href = url; link.download = 'user-avatar'; document.body.appendChild(link); link.click(); link.remove() }
  const remove = async (user: User) => { if (user.code === 'admin') return; try { await api.deleteUser(user.id); setChecked((ids) => ids.filter((id) => id !== user.id)); await load(); message.success('已删除') } catch (error) { message.error((error as Error).message) } }
  const batchDelete = async () => { try { await Promise.all(checked.map((id) => api.deleteUser(id))); setChecked([]); await load(); message.success('已删除选中用户') } catch (error) { await load(); message.error((error as Error).message) } }
  const columns = useMemo(() => [{ title: '序号', width: 70, render: (_: unknown, __: User, i: number) => i + 1 }, { title: '头像', dataIndex: 'avatar', width: 72, render: (avatar: string | null, row: User) => <Avatar size={32} src={avatar || undefined} icon={<UserOutlined />} alt={row.name} /> }, { title: '账号', dataIndex: 'code', render: (code: string, row: User) => <Typography.Text style={{ cursor: 'copy' }} onClick={(event) => { event.stopPropagation(); void copyToClipboard(code, '账号') }}>{code}</Typography.Text> }, { title: '显示名称', dataIndex: 'name' }, { title: '权限类别', dataIndex: 'role', render: (role: UserRole) => roleLabels[role] }, { title: '权限组', render: (_: unknown, row: User) => row.groups?.map((group) => group.name).join('、') || '未分配' }, { title: '操作', width: 150, render: (_: unknown, row: User) => <Space>{can('platform.user.write') && <Button type="link" onClick={() => edit(row)}>编辑</Button>}{can('platform.user.unlink') && <Popconfirm title="确认删除该用户？" onConfirm={() => void remove(row)} disabled={row.code === 'admin'}><Button type="link" danger disabled={row.code === 'admin'}>删除</Button></Popconfirm>}</Space> }], [users, groups, can])
  const filteredUsers = users.filter((row) => `${row.code} ${row.name}`.toLowerCase().includes(keyword.trim().toLowerCase()))
  return <div><div className="page-title"><div><Typography.Title level={3}>用户管理</Typography.Title><Typography.Text type="secondary">维护用户账号、头像、权限组与工作台访问权限</Typography.Text></div><Space>{can('platform.user.unlink') && <Button danger disabled={!checked.length} onClick={() => void batchDelete()}>删除选中</Button>}{can('platform.user.create') && <Button type="primary" onClick={create}>创建</Button>}</Space></div><Input.Search className="list-filter" allowClear placeholder="筛选账号或显示名称" value={keyword} onChange={(event) => setKeyword(event.target.value)} /><Table className="config-table" loading={loading} rowKey="id" columns={columns} dataSource={filteredUsers} scroll={{ x: 820 }} rowSelection={{ selectedRowKeys: checked, onChange: (keys) => setChecked(keys as number[]), getCheckboxProps: (row) => ({ disabled: row.code === 'admin' || !can('platform.user.unlink') }) }} pagination={{ pageSize: 20, showSizeChanger: true, pageSizeOptions: [10, 20, 50], showTotal: (total) => `共 ${total} 条` }} onRow={(record) => ({ onClick: (event) => { if ((event.target as HTMLElement).closest('button,.ant-popover,.ant-checkbox-wrapper')) return; if (can('platform.user.write')) edit(record) } })} /><Drawer title={selected ? '编辑用户' : '创建用户'} width={440} open={open} onClose={() => setOpen(false)} destroyOnClose extra={<Button type="primary" onClick={() => form.submit()}>保存</Button>}><Form form={form} layout="vertical" onFinish={(values) => void save(values)} autoComplete="off"><Form.Item label="头像" extra="显示使用 100KB 以内缩略图，下载按钮获取原图"><Form.Item noStyle shouldUpdate={(before, after) => before.avatar !== after.avatar}>{() => { const value = form.getFieldValue('avatar') as string | null | undefined; const original = form.getFieldValue('avatarOriginal') as string | null | undefined; return <Upload accept="image/*" beforeUpload={selectAvatar} showUploadList={false}>{value ? <div className="image-upload-card"><img src={value} alt="用户头像" /><div className="image-action image-download" title="下载原图" onClick={(event) => { event.preventDefault(); event.stopPropagation(); downloadAvatar(original || value) }}><DownloadOutlined /></div><div className="image-action image-delete" title="删除头像" onClick={(event) => { event.preventDefault(); event.stopPropagation(); form.setFieldsValue({ avatar: null, avatarOriginal: null }) }}><DeleteOutlined /></div></div> : <button type="button" className="image-upload-card image-upload-empty"><UploadOutlined /><span>点击上传</span></button>}</Upload> }}</Form.Item></Form.Item><Form.Item name="avatar" hidden><Input /></Form.Item><Form.Item name="avatarOriginal" hidden><Input /></Form.Item><Form.Item name="uuid" label="UUID"><Input readOnly placeholder="创建后自动生成" onClick={() => void copyToClipboard(selected?.uuid, 'UUID')} /></Form.Item><Form.Item name="code" label="账号" rules={[{ required: true }]}><Input readOnly={!!selected} autoComplete="off" onClick={() => { if (selected) void copyToClipboard(selected.code, '账号') }} /></Form.Item><Form.Item name="name" label="显示名称" rules={[{ required: true }]}><Input autoComplete="off" /></Form.Item><Form.Item name="password" label={selected ? '新密码（留空不修改）' : '密码'} rules={selected ? [] : [{ required: true }]}><Input.Password autoComplete="new-password" /></Form.Item><Form.Item name="role" label="兼容权限类别" rules={[{ required: true }]}><Select options={Object.entries(roleLabels).map(([value, label]) => ({ value, label }))} disabled={selected?.code === 'admin'} /></Form.Item><Form.Item name="groupIds" label="权限组" extra="权限组决定菜单、CRUD 和后续数据范围；可多选"><Select mode="multiple" options={groups.map((group) => ({ value: group.id, label: `${group.name} (${group.code})` }))} /></Form.Item></Form></Drawer></div>
}
