## 任务与分支管理

1. 每个任务在修改代码前必须创建 Git 分支。
2. 开发任务完成后**更新**README.md（系统介绍）
3. 分支命名：`YYYYMMDDNNN`（年月日 + 3位流水号），每次新分支在当前所在分支上创建并比前一个分支流水号上 +1，若今天是新的一天且无当日DD分支，则创建当日YYYYMMDD001，如果不知道当前分支的流水号应该给多少，可以直接查看git branch的历史。
4. (不自动，需询问）Commit 格式：`[ADD/IMP/FIX]本次变更代码功能说明#task:YYYYMMDD00`，询问用户是否需要Commit。
   - ADD：新增
   - IMP：功能优化
   - FIX：BUG修复
5. (不自动，需询问）功能开发完成后默认将当前分支PR到origin，PR最好是带上title、desc，可以直接上去PR不需要编辑维护标题和内容信息，直接带出。询问用户是否需要Push与PR。

## 工程架构约束

整个工程采用 Monorepo 的模块化架构思想。平台内核、公共能力和具体行业业务必须保持清晰边界；新增功能不得继续无边界堆积到单一入口文件、全局 API 文件或全局配置表中。

系统架构、目录分层、模块边界、依赖方向、数据库迁移、权限和多租户规则统一以 [`docs/系统架构.md`](docs/系统架构.md) 为准。所有后续开发、重构和新模块接入都必须遵守该文档；如确需偏离，必须在变更说明和架构文档中记录原因、影响范围及替代方案。

## 页面开发约束
1. 所有按钮文字前不要增加任图标;
2. list界面必须有:序号\筛选\分页\多选\批量操作\单双行有底色区分\记录行的高度统一略比显示文字高一点,保持美观;
3. form界面默认右侧抽屉页处理,除非特别说明整页维护,否则都为右侧抽屉页;
4. 所有的图片维护都是点击上传,图片框左上方下载,右上方删除,详细可参考系统配置功能的logo图片维护方式;
