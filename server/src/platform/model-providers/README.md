# 模型供应商

负责维护平台可用的大模型供应商配置，当前支持 OpenAI 兼容协议的预设厂商与自定义 HTTPS 服务地址。

- API Key 使用 AES-256-GCM 加密后存入 `model_providers.api_key_encrypted`，列表接口不会返回明文。
- “测试连接”和“获取模型”调用供应商的 `GET /models` 接口；请求仅允许解析到公网 HTTPS 地址，避免配置被用于访问本机或内网。
- 其他模块只能经本模块的服务接口使用供应商配置，不能直接读取数据表或密钥字段。
